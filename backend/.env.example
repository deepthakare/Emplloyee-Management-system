# Example local connection (uncomment one style)
# PG_URI=postgres://postgres:postgres@localhost:5432/ems

# Or provide discrete parts instead of URI
PGHOST=localhost
PGPORT=5432
PGDATABASE=ems
PGUSER=postgres
PGPASSWORD=postgres

# Server
PORT=4000
CORS_ORIGIN=http://localhost:5173
